[project]
name = "asase-ai-agent"
version = "0.1.0"
description = "Custom AI agent - can be run with public or local Ollama LLM"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "google-genai>=2.10.0",
    "python-dotenv>=1.2.2",
    "requests>=2.32.3",
]
